## 1. DTO Updates

- [x] 1.1 Add `back_default` field to `Sprites` class in `PokeApiPokemonResponse.java`.
- [x] 1.2 Add `back_default` field and annotations to `Sprites` class in `PokemonResponse.java`.

## 2. Service Update

- [x] 2.1 Update `PokemonService.getPokemonByName` to map the `back_default` field.

## 3. Verification

- [x] 3.1 Run `mvn clean compile` to ensure no errors were introduced.
