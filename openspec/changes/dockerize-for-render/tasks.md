## 1. Docker Files

- [x] 1.1 Create `.dockerignore` to exclude `target`, `.git`, etc.
- [x] 1.2 Create `Dockerfile` with multi-stage build (build and runtime).

## 2. Verification

- [x] 2.1 (Local) Attempt to build the image if Docker is available (optional but recommended).
- [x] 2.2 Verify that the application structure in `project.md` includes Docker information.
