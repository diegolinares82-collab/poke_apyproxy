## 1. Configuration

- [x] 1.1 Add `cors.allowed-origins=*` to `application.properties`.
- [x] 1.2 Create `WebConfig.java` in `com.example.pokeproxy.config` package.
- [x] 1.3 Implement `addCorsMappings` in `WebConfig.java` using the property from 1.1.

## 2. Verification

- [x] 2.1 Run `mvn compile` to ensure no errors were introduced.
- [x] 2.2 (Manual) Verify that the API still works locally.
