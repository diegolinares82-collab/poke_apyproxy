## 1. DTO Updates

- [x] 1.1 Add `effort` field to `StatEntry` in `PokeApiPokemonResponse.java`.
- [x] 1.2 Add `effort` field and `@Schema` annotation to `StatEntry` in `PokemonResponse.java`.

## 2. Service Update

- [x] 2.1 Update `PokemonService.getPokemonByName` to map the `effort` field for each stat.

## 3. Verification

- [x] 3.1 Run `mvn clean compile` to ensure no errors were introduced.
